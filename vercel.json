{
  "build": {
    "env": {},
    "commands": {
      "build": "npx parcel build src/index.html --dist-dir dist"
    },
    "outputDirectory": "dist"
  },
  "rewrites": [{ "source": "/(.*)", "destination": "/" }]
}
